"use client";
import {useEffect,useState} from "react";
import Link from "next/link";
import {ArrowLeft,UserRound} from "lucide-react";
import {supabase} from "../../../../lib/supabaseClient";
export default function Followers({params}){const [profile,setProfile]=useState(null),[followers,setFollowers]=useState([]),[status,setStatus]=useState("");useEffect(()=>{async function load(){const {data:p}=await supabase.from("user_profiles").select("*").or(`username.eq.${params.username},id.eq.${params.username}`).maybeSingle();if(!p){setStatus("Profile not found.");return}setProfile(p);const {data:rows}=await supabase.from("profile_follows").select("follower_id").eq("following_id",p.id).order("created_at",{ascending:false});let ids=(rows||[]).map(r=>r.follower_id);if(!ids.length)return;const {data:people}=await supabase.from("user_profiles").select("*").in("id",ids);setFollowers(people||[])}load()},[params.username]);return <main className="social-home-page"><Link className="back-link" href={profile?`/profile/${profile.username||profile.id}`:"/creator-hub"}><ArrowLeft/> Back</Link><section className="hub-feed-panel followers-page-panel"><h1>{profile?.display_name||"Profile"} Followers</h1>{status&&<p className="hub-status">{status}</p>}{!status&&!followers.length&&<p>No followers yet.</p>}{followers.map(person=><article className="creator-mini-card" key={person.id}><div className="mini-avatar">{person.avatar_url?<img src={person.avatar_url} alt=""/>:<UserRound/>}</div><div><strong>{person.display_name||"FlashPortal User"}</strong><span>@{person.username||"user"}</span><Link href={`/profile/${person.username||person.id}`}>View Profile</Link></div></article>)}</section></main>}
